# fpcalc (Chromaprint) dla bundlowania

Aby dołączyć `fpcalc.exe` do wersji portable:

1. Skopiuj `fpcalc.exe` do `tools/`.
2. Uruchom `build/build.ps1`.

Alternatywnie: jeśli `fpcalc` jest zainstalowany i widoczny w `PATH`,
skrypt budowania automatycznie go wykryje i skopiuje do `dist`.
